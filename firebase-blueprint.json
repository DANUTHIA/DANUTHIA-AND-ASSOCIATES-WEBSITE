{
  "entities": {
    "Project": {
      "title": "Project",
      "description": "An architectural project in the portfolio.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "category": { "type": "string" },
        "location": { "type": "string" },
        "year": { "type": "string" },
        "img": { "type": "string" },
        "images": { 
          "type": "array",
          "items": { "type": "string" }
        },
        "description": { "type": "string" },
        "client": { "type": "string" },
        "area": { "type": "string" },
        "status": { "type": "string" },
        "collaborators": { "type": "string" }
      },
      "required": ["title", "category", "location", "year", "img", "description", "client", "area", "status"]
    }
  },
  "firestore": {
    "projects": {
      "schema": { "$ref": "#/entities/Project" },
      "description": "Collection of portfolio projects"
    }
  }
}
